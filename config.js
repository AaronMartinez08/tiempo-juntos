// Claves del proyecto de Firebase (Realtime Database). No son contraseñas: son
// identificadores públicos del proyecto. Lo que protege los datos son las reglas
// de seguridad de la base de datos, no mantener esto en secreto.
export const FIREBASE_DB_URL = 'https://tiempojuntos-default-rtdb.firebaseio.com';

// Nombres de las dos personas en la base de datos. No hace falta que coincidan
// con los nombres que se ven en la app (esos se guardan aparte, en "perfiles").
export const ROLES = ['A', 'B'];
